<!-- lessonsData is for all the lessons db -->
<!-- lesson is just the information passed by the ID (this specific lesson) -->
<%- include('partials/header'); %>
<section id="indicators">
    <h3>Here are our comprehensive Trading Suites</h3>
    <div class="container">
        <div id="best5">
            <div class="subtitle">
                <h2>Price Action Package: </h2><a href="/#pricing" class="button primary">buy now</a>
            </div>
            <% top5.forEach(indicator => { %>
            <div class="indicator">
                <div class="text">
                    <a href="<%= indicator.tv %> ">
                        <h2><%= indicator.name %> </h2>
                        <p><%= indicator.description %> </p>
                    </a>
                </div>
                <img src="images/indicators/<%= indicator.thumbnail %>.png" alt="">
            </div>
            <% }) %>
        </div>
        <div id="best10">
            <div class="subtitle">
                <h2>Scalping Package: </h2><a href="/#pricing" class="button primary">buy now</a>
            </div>
            <% top10.forEach(indicator => { %>
            <div class="indicator">
                <div class="text">
                    <a href="<%= indicator.tv %> ">
                        <h2><%= indicator.name %> </h2>
                        <p><%= indicator.description %> </p>
                    </a>
                </div>
                <img src="images/indicators/<%= indicator.thumbnail %>.png" alt="">
            </div>
            <% }) %>
        </div>
        <div class="subtitle">
            <h2>For Everything Else: </h2><a href="/#pricing" class="button primary">buy now</a>
        </div>

        <div id="theRest">
            <% top20.forEach(indicator => { %>
            <div class="indicator">
                <a href="<%= indicator.tv %> ">
                    <p><%= indicator.name %> </p>
                </a>
            </div>
            <% }) %>
        </div>

    </div>
</section>
<%- include('partials/footer'); %>